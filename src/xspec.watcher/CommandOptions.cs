namespace xspec.watcher
{
	public enum CommandOptions
	{
		/// <summary>
		/// The path that contains the solution file and code files for monitoring.
		/// </summary>
		Path,

		/// <summary>
		/// Option to allow the runner to provide visual feedback on the tests (like Growl)
		/// </summary>
		FeedBack
	}
}