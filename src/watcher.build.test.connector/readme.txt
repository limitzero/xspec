To register to the build and test connector for watcher, just drop 
the watcher.build.test.connector.dll, build.cmd, test.cmd in the same directory as the watcher.exe 
and restart watcher.exe, the watcher configuration will pick up all new connector pipelines on start.

